#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_line(int n)
{
	int i;
	int n;

	for (i; i <= n ; i++)
	{
		if (n > 0)
		{
			_putchar('_');
		_putchar('\n');
		}
		if (n <= 0)
		{
			_putchar('\n');
		}
	}
}
